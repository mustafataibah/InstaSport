<?xml version="1.0" encoding="utf-8"?>
<resources>

    // Ignore better to use Material 3 in ui.theme folder under root
    <style name="Theme.InstaSport" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:textColor">@color/white</item>
        <item name="android:windowBackground">@color/dark_purple</item>
        <item name="buttonStyle">@style/Theme.Button</item>
        <item name="editTextStyle">@style/Theme.EditText</item>
    </style>

    <style name="Theme.Button" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:backgroundTint">@color/light_purple</item>
    </style>

    <style name="Theme.EditText" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:background">@color/white</item>
        <item name="android:textColor">@color/black</item>
    </style>

</resources>